# Routine update — systemd
#
# No editing needed. The first deploy created deploy.env in your
# project root with the SSH address, server path, port, and service name.
# Paste this to your AI agent each time you push new code.

Read deploy.env, then update the app:
1. SSH to DEPLOY_SSH, cd to DEPLOY_PATH.
2. git pull
3. bun install (only if package.json changed — check git diff)
4. bun run build
5. sudo systemctl restart DEPLOY_SERVICE
6. Verify: curl http://localhost:DEPLOY_PORT/health → 200
7. Verify ClickHouse: curl http://localhost:8123/ping → "Ok."
8. Tell me the status and any issues.
